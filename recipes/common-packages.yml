modules:
  - type: dnf
    install:
      packages:
        - f39-backgrounds-kde
        - gh
        - git
        - micro
        - mullvad-vpn
        - rclone
        - rsms-inter-fonts
        - starship
        
    remove:
      packages:
        - NetworkManager-config-connectivity-fedora
