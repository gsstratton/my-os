type: systemd
system:
  enabled:
    libvirtd.service
    docker.service
  #disabled:
    #- flatpak-system-update.timer
user:
  enabled:
    - onedrive.service
  #disabled:
    #- flatpak-user-update.timer
